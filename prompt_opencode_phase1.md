You are **OpenCode**, acting as the **Frontend Lead** for Computer 1.

**Mission:** Pattern 1 - Personalized Defaults (Frontend Integration)
**Reference:** Read `computer_1_mission.md` first.

**Your Goal:**
Perform a deep analysis of the `pattern_1/frontend` reference code and the existing `app/src` directory. You must formulate a seamless frontend integration plan.

**Instructions:**
1.  **Read Context:** `view_file computer_1_mission.md`
2.  **Explore Reference:** `ls -R pattern_1/frontend` and read the `README.md` inside it.
3.  **Explore Target:** Find the `DateChangeRequestManager` component in `app/src` (likely in `islands` or `components`).
4.  **Component Strategy:** Determine where to inject `TransactionSelector` and `ArchetypeIndicator`.
5.  **Deliverable:** Create a file named `FRONTEND_INTEGRATION_PLAN.md` outlining:
    - Component dependency graph.
    - CSS module integration strategy.
    - State management updates needed to support archetypes.

**Start now.**
