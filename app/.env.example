# ============================================
# SPLIT LEASE - FRONTEND ENVIRONMENT
# ============================================
# Copy this file: cp .env.example .env
# Then fill in your actual values.

# SUPABASE (Required)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# GOOGLE MAPS (Required)
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# STRIPE (Required for payments)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your-key-here

# BUBBLE.IO (Required for listing photo uploads)
# NOTE: Frontend uses VITE_BUBBLE_API_KEY. Python backend uses BUBBLE_API_TOKEN
# and BUBBLE_API_KEY (see pythonanywhere/mysite/.env.example). These are
# different credentials for different API layers but serve the same Bubble app.
VITE_BUBBLE_API_KEY=your-bubble-api-key-here

# MONITORING (Optional)
VITE_SENTRY_DSN=
VITE_HOTJAR_SITE_ID=
VITE_SLACK_ERROR_WEBHOOK=
VITE_APP_VERSION=

# FEATURE FLAGS (Optional — defaults shown)
# VITE_ENABLE_SMART_PRICING=true
# VITE_ENABLE_SMART_DATES=true
# VITE_ENABLE_ANALYTICS=true
